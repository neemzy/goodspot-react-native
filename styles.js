import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  bold: {
    fontWeight: "bold"
  },
  paragraph: {
    padding: 10
  },
  field: {
    minHeight: 40,
    margin: 10
  }
});

export default styles;
